# macOS
.DS_Store

# Go
daemon/go.sum
npm/binaries/

# Node
node_modules/
npm-debug.log*

# Extension build output
extension/dist/
extension/out/
extension/webview/vendor/
extension/*.vsix

# vscode-test-web downloaded VS Code build (large, re-downloaded on demand)
extension/.vscode-test-web/

# npm package-lock files (not needed for a monorepo without a root package.json)
package-lock.json
npm/package-lock.json
extension/package-lock.json
