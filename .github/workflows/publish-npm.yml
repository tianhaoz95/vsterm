name: Publish daemon npm package

on:
  push:
    branches: [main]
    paths:
      - 'npm/package.json'

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get.outputs.version }}
      skip_github: ${{ steps.check_github.outputs.skip }}
      skip_npm: ${{ steps.check_npm.outputs.skip }}
    steps:
      - uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Get version from npm/package.json
        id: get
        run: echo "version=$(jq -r .version npm/package.json)" >> "$GITHUB_OUTPUT"

      - name: Check if already published to GitHub Packages
        id: check_github
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.get.outputs.version }}"
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            "https://npm.pkg.github.com/@tianhaoz95/vsterm/$VERSION")
          if [ "$STATUS" = "200" ]; then echo "skip=true"; else echo "skip=false"; fi >> "$GITHUB_OUTPUT"

      - name: Check if already published to npm registry
        id: check_npm
        run: |
          VERSION="${{ steps.get.outputs.version }}"
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://registry.npmjs.org/@tianhaoz95/vsterm/$VERSION")
          if [ "$STATUS" = "200" ]; then echo "skip=true"; else echo "skip=false"; fi >> "$GITHUB_OUTPUT"

  build:
    needs: check-version
    if: needs.check-version.outputs.skip_github == 'false' || needs.check-version.outputs.skip_npm == 'false'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: daemon/go.mod
          cache-dependency-path: daemon/go.sum
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Run daemon tests
        working-directory: daemon
        run: go test -timeout 30s ./...

      - name: Build binaries for all platforms
        run: |
          mkdir -p npm/binaries
          cd daemon
          GOOS=darwin  GOARCH=arm64 go build -o ../npm/binaries/vsterm-darwin-arm64  .
          GOOS=darwin  GOARCH=amd64 go build -o ../npm/binaries/vsterm-darwin-x64    .
          GOOS=linux   GOARCH=arm64 go build -o ../npm/binaries/vsterm-linux-arm64   .
          GOOS=linux   GOARCH=amd64 go build -o ../npm/binaries/vsterm-linux-x64     .
          GOOS=windows GOARCH=amd64 go build -o ../npm/binaries/vsterm-win32-x64.exe .

      - name: Verify binary sizes
        run: |
          for f in npm/binaries/*; do
            size=$(stat -c%s "$f")
            echo "$f: $(( size / 1024 / 1024 )) MB"
            [ "$size" -lt 15728640 ] || (echo "ERROR: $f exceeds 15 MB limit" && exit 1)
          done

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Run npm tests
        working-directory: npm
        run: |
          npm install
          VSTERM_SKIP_SERVICE=1 npm test

      - name: Upload binaries artifact
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: npm/binaries/
          retention-days: 1
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

  publish-github:
    needs: [check-version, build]
    if: needs.check-version.outputs.skip_github == 'false'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Download binaries
        uses: actions/download-artifact@v4
        with:
          name: binaries
          path: npm/binaries/
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Configure registry → GitHub Packages
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@tianhaoz95'
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Publish to GitHub Packages
        working-directory: npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VSTERM_SKIP_SERVICE: '1'
        run: npm publish

  publish-npm:
    needs: [check-version, build]
    if: needs.check-version.outputs.skip_npm == 'false'
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Download binaries
        uses: actions/download-artifact@v4
        with:
          name: binaries
          path: npm/binaries/
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Configure registry → npmjs.org
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          scope: '@tianhaoz95'
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Publish to npm registry
        working-directory: npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
          VSTERM_SKIP_SERVICE: '1'
        run: npm publish
